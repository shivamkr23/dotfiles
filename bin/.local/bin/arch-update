#!/usr/bin/env bash

function gohome() {
    "cd"
}

gohome
bash -c "yay"
bash -c "yarn global upgrade"
bash -c "pnpm up -g"
bash -c "go-global-update"
bash -c "cargo install-update --all"
bash -c "pipx upgrade-all --global"
